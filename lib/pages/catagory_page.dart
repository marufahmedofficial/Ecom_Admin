import 'package:flutter/material.dart';

class CatagoryPage extends StatelessWidget {
  const CatagoryPage({Key? key}) : super(key: key);
  static const String routeName= '/CatagoryPage';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
